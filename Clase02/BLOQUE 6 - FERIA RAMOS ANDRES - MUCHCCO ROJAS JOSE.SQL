--============ BLOQUE 6

--1
SELECT * FROM EDUCA.CURSO WHERE  NVL(TRIM(CUR_PROFESOR),' ')=' ';

--2
SELECT * FROM RECURSOS.EMPLEADO;
SELECT IDEMPLEADO,APELLIDO,NOMBRE, SUELDO + NVL(COMISION,0) AS SUELDO_TOTAL FROM RECURSOS.EMPLEADO;

--3
SELECT * FROM RECURSOS.EMPLEADO;
SELECT IDEMPLEADO,APELLIDO,NOMBRE, SUELDO + NVL(COMISION,0) AS SUELDO_TOTAL FROM RECURSOS.EMPLEADO WHERE (SUELDO + NVL(COMISION,0)) < 8000

--4)
--a)
SELECT * FROM SCOTT.EMP;
SELECT ename,sal+nvl(comm,0) AS ingreso FROM scott.EMP WHERE (sal+nvl(comm,0))<3500;
--b)
SELECT nombre,sueldo+nvl(comision,0) FROM recursos.EMPLEADO WHERE (sueldo+nvl(comision,0))<3500