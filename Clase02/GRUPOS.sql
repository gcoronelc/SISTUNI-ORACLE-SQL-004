-- FUNCIONES AGREGADAS

-- COUNT

SELECT COUNT(*) FROM SCOTT.EMP;

SELECT 
	COUNT(*) FILAS,
	COUNT(EMPNO) AAAA,
	COUNT(DEPTNO) BBBB,
	COUNT(COMM)  CCCC
FROM SCOTT.EMP;




SELECT 
	COUNT(DEPTNO) BBBB,
	COUNT(DISTINCT DEPTNO)  CCCC
FROM SCOTT.EMP;

SELECT DISTINCT DEPTNO FROM SCOTT.EMP;

SELECT COUNT(*) AAA, COUNT(1) BBBB
FROM SCOTT.EMP;

SELECT COUNT(*) AAA, COUNT(1) BBBB, SUM(1) CCCC
FROM SCOTT.EMP;

-- SUM

SELECT SUM(SAL) PLANILLA FROM SCOTT.EMP;

-- 29025



SELECT 
	DEPTNO,
	COUNT(1) EMPS
FROM SCOTT.EMP
WHERE DEPTNO IS NOT NULL
GROUP BY DEPTNO
HAVING COUNT(1) < 5;



-- AVG

SELECT AVG(NVL(SAL,0)) PLANILLA FROM SCOTT.EMP;

-- 2073.21

-- MAX y MIN

SELECT 
	MIN(HIREDATE) MINIMO, 
	MAX(HIREDATE) MAXIMO
FROM SCOTT.EMP;









